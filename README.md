# datadog-apm-azure

Note: If you don’t need a particular Datadog feature (for example: APM, logs, service monitoring, network monitoring, process agent, etc.), you can disable it by changing true to false. This helps reduce resource usage and keeps the agent lightweight.
